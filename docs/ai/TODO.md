# RocoTools TODO

> 更新时间：2026-07-24
> 当前状态：committed
> 当前优先级：文档一致性 → 功能实现 → CI / 性能 / 安全专项
> 当前执行指向：先合并并验证文档分支，再从最新 `main` 创建精灵对比功能分支。

## 已确认的工作原则

- 先修正文档漂移，确保 README、脚本文档、架构说明和 AI 上下文与当前代码一致。
- 文档稳定后优先实现产品功能。
- CI、自动化测试体系、性能优化和安全专项暂缓，后续统一规划。
- 暂缓安全专项不代表允许新增风险；新功能不得扩大现有认证、公告 HTML、反馈附件、上传、备份或数据库风险。
- 每个功能使用独立分支、独立验证和独立交接。
- 不把 `planned` 写成 `committed` 或 `deployed`。

## P0：当前必须完成

### 1. 确认生产基线

- [x] 确认服务器实际分支为 `main`（用户于 2026-07-24 根据服务器现状确认）。
- [ ] 记录服务器实际 commit SHA。
- [x] 审查从服务器下载的 `deploy.sh`，并确认其与仓库根目录本机忽略副本正文一致。
- [ ] 确认 PM2 当前进程名、实例数和状态。
- [ ] 确认线上构建与具体 Git commit 的对应关系。

当前证据：

- 服务器分支由用户确认为 `main`。
- 2026-07-24 审查的服务器脚本副本固定进入 `/var/www/roco`，执行 `git pull origin main`。
- 该脚本只在 `app/client/` 有提交差异时重新构建前端，只在 `app/server/` 有提交差异时安装生产依赖，但每次有效部署都会 reload 名为 `roco` 的 PM2 服务。
- 该脚本不运行 `sync_db.js`，不检查或同步 SQLite；生产数据由用户更新后主动同步回本地，本 TODO 不再要求核验生产 SQLite 完整性或数据量。
- `deploy.sh` 已在 `07f830f` 从 Git 追踪移除并加入 `.gitignore`；服务器脚本是独立运维文件，不会随 `git pull main` 自动更新。
- 2026-07-24 只读 SSH 连接仍被服务器拒绝（`Permission denied (publickey,password)`），因此服务器 SHA、PM2 实际状态和线上构建对应 commit 仍为 `needs-verification`。
- 公开站点旁证显示线上构建时间约晚于 `419a62a` 4 分钟，且线上邻居 API 与精灵详情构建均包含该提交引入的 `avatar_url`；可确认 Q 版头像已部署，但仍不能替代生产 SHA。

### 2. 合并可移植工程上下文

- [x] 审核 `eachzhang/codex/context-handoff`。
- [x] 已决定并 fast-forward 合并到 `main`，远程 `main` 为 `848207a`。
- [ ] 合并后从新任务验证根目录 `AGENTS.md` 自动生效。
- [x] 验证 `$rocotools-context-handoff` 可以从仓库 Skill 列表调用。
- [ ] 验证新电脑能按 `docs/ai/START_HERE.md` 恢复上下文。

### 3. 解决文档漂移

#### README

- [x] 将当前数据口径统一为 374 个去重精灵、494 个形态、495 个技能、18 个属性、15 个蛋组、30 个性格。
- [x] 明确普通 `node sync_db.js` 默认生成图片衍生物并建表/补列，但不导入 JSON。
- [x] 明确只有 `node sync_db.js --full` 才执行完整数据导入和后处理。
- [x] 修正被 Git 忽略的旧部署文档所产生的无效跨电脑链接。
- [x] 区分当前功能、历史版本和未来规划。

#### SCRIPTS.md

- [x] 处理仅行尾层面的工作区差异，确认正文哈希未变化。
- [x] 删除 `b1f2a83` 已移除的旧 API 测试脚本说明。
- [x] 修正 `sync_db.js` 默认模式和 `--full` 模式。
- [x] 区分代码部署、数据同步、完整导入和服务器数据拉取。
- [x] 修正把 `git push` 等同生产部署的错误描述，以服务器实际部署机制为准。

#### 部署文档

- [x] 将脱敏后的正式部署说明迁移到受 Git 管理的 `docs/operations/DEPLOY.md`。
- [x] 保持真实用户名、服务器地址、密钥、密码和 `.env` 在 Git 外。
- [x] 记录服务器实际 `deploy.sh` 行为。
- [x] 明确前端构建会覆盖 `app/server/public/`。
- [x] 明确什么时候 reload PM2，什么时候不需要 reload。
- [x] 明确当前部署不执行数据库备份、完整性校验或 `sync_db.js`。
- [x] README 和工程上下文统一指向新的正式部署文档。

#### 功能与架构文档

- [x] 将 `docs/features/feedback.md` 更新为 `deployed`，并补充真实入口、API、存储和风险。
- [x] 更新 `docs/architecture/README.md` 中的旧统计为当前口径。
- [x] 更新 `app/README.md` 中 `sync_db.js` 的旧行为描述。
- [x] 更新 `.ai-memory.md` 中的旧统计、部署描述、敏感源站地址和 Service Worker 待办。
- [x] 保留 CHANGELOG 的历史事实；旧统计标明当时口径，不直接改写历史。
- [x] 补充 Q 版头像相关 CHANGELOG 记录。
- [x] 更新 `DOC_RULES.md` 的正式文档索引。
- [x] 全仓搜索并消除受维护文档中冲突的状态、数量、路径和命令。

#### AI 状态与风险

- [x] 文档修复完成后更新 `docs/ai/STATUS.md`。
- [x] 更新 `docs/ai/HANDOFF.md` 的分支、基准、验证和下一步。
- [x] 所有相关文档完成核验后，将 `RISK_REGISTER.md` 的 M-06 更新为 `mitigated`。

### 4. 文档修复验收

- [x] `git diff --check` 通过。
- [x] 所有受 Git 管理的 Markdown 本地链接存在。
- [x] 受维护文档不再引用已删除的旧 API 测试脚本。
- [x] 全仓对 `sync_db.js` 默认行为描述一致。
- [x] 当前数据统计口径一致；CHANGELOG 与爬虫报告保留并标注历史/生成快照口径。
- [x] 不包含密码、Token、JWT 密钥、生产数据库或用户隐私数据。
- [x] 文档修改不触及产品源码、数据库 schema 或运行数据。
- [x] 提交并推送独立文档分支（实现提交 `7735ecb`）。

### 5. 文档目录整理

- [x] 新增 `docs/README.md` 文档中心。
- [x] 将架构、独立功能、开发规范和展示标准正文归入分类目录。
- [x] 为 7 个旧路径保留兼容入口。
- [x] 更新 README、DOC_RULES、AI memory、开发 Skills 和 AI 上下文中的权威路径。
- [x] 确认迁移后的 7 份正文内容一致；仅清理架构文档 1 行既有尾随空格。
- [x] 所有受 Git 管理及本轮新增 Markdown 本地链接存在。
- [x] 上下文验证、`git diff --check` 和非文档变更检查通过。
- [x] 提交并推送文档目录整理（实现提交 `4ff7ac4`）。

### 6. 文档分支收口

- [x] `eachzhang/codex/docs-drift` 已推送并与远程跟踪分支一致。
- [x] 确认 `main..eachzhang/codex/docs-drift` 仅包含文档提交，不含业务源码、schema 或运行数据；实时提交数以 Git 为准。
- [ ] 审核 `main..eachzhang/codex/docs-drift` 的最终差异。
- [ ] fast-forward 合并到 `main` 并推送远程。
- [ ] 合并后从新任务验证 `docs/README.md`、根 `AGENTS.md` 和 `$rocotools-context-handoff`。
- [ ] 将文档一致性修复和目录整理状态更新为已进入 `main`。

## P1：优先实现产品功能

### 1. 精灵对比

- [ ] 文档分支合并后，从最新 `main` 创建独立功能分支。
- [ ] 核对现有精灵列表、详情 API、`PetPicker`、`StatsRadar` 和属性顺序实现，确认可复用边界。
- [ ] 定义首期范围和验收标准。
- [ ] 首期建议限定同时对比 2 只精灵；确认后再实现。
- [ ] 确定对比字段：属性、种族值、技能、特性、蛋组、标签或其他资料。
- [ ] 确定多形态选择规则。
- [ ] 确定 URL 状态和分享方式。
- [ ] 增加用户端入口和路由。
- [ ] 完成移动端、平板和桌面布局。
- [ ] 保持现有排序、属性顺序和 UID 规则。
- [ ] 完成构建与人工冒烟验证。
- [ ] 更新 README、CHANGELOG、STATUS 和 HANDOFF。

### 2. 用户端 Excel 导出

- [ ] 明确与现有管理端整库 Excel 导出的区别。
- [ ] 确定导出对象：单精灵、多个精灵、技能或分析结果。
- [ ] 确定导出字段和展示顺序。
- [ ] 优先评估纯前端生成，避免新增服务器存储。
- [ ] 确定文件名、中文兼容和移动端下载行为。
- [ ] 完成构建与人工导出验证。

### 3. 用户端 PDF 报告

- [ ] 在 Excel 导出范围稳定后再定义 PDF。
- [ ] 确定报告模板、分页、字体和图片策略。
- [ ] 确定是否支持单精灵、对比报告和分析报告。
- [ ] 控制图片和字体体积。
- [ ] 完成桌面端与移动端下载验证。

### 4. 已提交功能的线上确认

- [x] 确认 Q 版头像已经部署（公开邻居 API 与线上构建均包含 `avatar_url`）。
- [ ] 确认 BWIKI 新版适配对应的线上 commit。
- [ ] 确认命定花种增强对应的线上 commit。
- [ ] 重新定义历史“数据导出”待办，避免与管理端导出重复。

推荐功能顺序：

```text
精灵对比
→ 用户端 Excel 导出
→ 用户端 PDF 报告
```

## P2：后续统一处理

### CI 与自动化

- [ ] GitHub Actions 基础验证。
- [ ] 前端构建和路由冒烟测试。
- [ ] 管理 API 未登录拒绝测试。
- [ ] PWA、manifest 和 SEO 产物检查。
- [ ] 爬虫本地 HTML fixture 解析测试。
- [ ] SQLite 只读约束和数据一致性检查。
- [ ] 跨平台 Git hooks 和行尾规范。

### 性能

- [ ] 前端包体和路由拆包评估。
- [ ] 超大 Vue 页面拆分。
- [ ] 后端查询和缓存评估。
- [ ] 图片、PWA、CDN 和缓存策略统一评估。
- [ ] PM2、Nginx 和日志策略评估。

### 安全

- [ ] H-01 管理认证 fail-open。
- [ ] H-02 公告存储型 XSS。
- [ ] H-03 反馈附件公开访问。
- [ ] H-04 数据库恢复直接覆盖活动库。
- [ ] H-05 上传及生产依赖审计。
- [ ] M-01～M-05 中风险统一整改。

## 每项任务的完成定义

- [ ] 当前代码和 Git 历史验证完成。
- [ ] 状态使用 `deployed`、`committed`、`working-tree`、`planned` 或 `deprecated`。
- [ ] 相关构建、语法、只读数据或人工冒烟检查通过。
- [ ] 未验证项和外部依赖已明确记录。
- [ ] `HANDOFF.md` 已记录分支、commit、改动、验证和下一步。
- [ ] 功能或风险状态变化时同步更新 `STATUS.md` 或 `RISK_REGISTER.md`。
- [ ] 需要跨电脑继续的工作已经提交并推送到功能分支。
